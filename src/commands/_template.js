const Discord = require(`discord.js`);
const log = require(`../modules/log.js`);

module.exports = {
	name: ``,
	description: ``,
	category: ``,
	args: false,
	guildOnly: false,
	devOnly: false,
	hide: false,
	cooldown: 0,
	aliases: [],
	usage: ``,
	async execute(client, message, args) {

	},
}