const Discord = require(`discord.js`);
const log = require(`../modules/log.js`);

module.exports = {
    name: ``,
    description: ``,
    category: ``,
    args: false,
    guildOnly: false,
    devOnly: false,
    hide: false,
    aliases: [],
    usage: ``,
    async execute (client, message, args) {

    },
}